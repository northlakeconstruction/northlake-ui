/**
 * Featured projects / testimonials.
 * These appear in the Featured Projects section of the homepage.
 */
/**
 * Add entries here as the client completes projects.
 * When this array is non-empty, Featured Projects automatically
 * appears in the navigation and on the homepage.
 */
export const featuredProjects: {
  quote: string
  byline: string
  detailLink: string
  image: { src: string; alt: string }
}[] = []
